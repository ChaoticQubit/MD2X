{
  "name": "md2pdf",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only Mermaid renderer + Pandoc PDF pipeline. No globals.",
  "scripts": {
    "render": "mmdc"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.0"
  }
}

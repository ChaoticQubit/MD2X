{
  "name": "md2x",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only Mermaid renderer for md2x (Markdown to PDF/DOCX/HTML/EPUB/LaTeX). No globals.",
  "scripts": {
    "render": "mmdc"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.0"
  }
}
